# @package _global_

defaults:
  - _self_
  - data: datamodule # choose datamodule with `test_dataloader()` for evaluation
  - model: trigram_module
  - logger: null
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: D:\RepoBitbucket\light-chat\W_parameter.pth
